model Rental{
    id Int @id @default(autoincrement())
    createdAt    DateTime    @default(now())
    updatedAt    DateTime?   @updatedAt
    status       RENTALSTATUS
    dateEndRental DateTime
    dateStartRental DateTime
    
}
enum RENTALSTATUS {
    LASTS
    CANCELLED
    COMPLETED
}
